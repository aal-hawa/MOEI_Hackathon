import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import SuppressRechartsWarnings from "@/components/dashboard/SuppressRechartsWarnings";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MOEI Country Intelligence Platform",
  description: "AI-powered, source-verified country intelligence for the Ministry of Energy & Infrastructure. No hallucination.",
  keywords: ["MOEI", "Country Intelligence", "Ministry of Energy", "Infrastructure", "UAE"],
  authors: [{ name: "Ministry of Energy & Infrastructure" }],
  icons: {
    icon: "/favicon.png",
    shortcut: "/favicon.png",
    apple: "/favicon.png",
  },
  openGraph: {
    title: "MOEI Country Intelligence Platform",
    description: "AI-powered, source-verified country intelligence",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <head>
        <link rel="icon" href="/favicon.png" />
        <link rel="shortcut icon" href="/favicon.png" />
        <link rel="apple-touch-icon" href="/favicon.png" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <SuppressRechartsWarnings />
        {children}
        <Toaster />
      </body>
    </html>
  );
}
